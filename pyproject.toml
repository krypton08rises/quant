[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "quant_fly"
version = "0.0.1"
requires-python = ">=3.10"
dependencies = []

[project.readme]
file = "README.md"
content-type = "text/markdown"